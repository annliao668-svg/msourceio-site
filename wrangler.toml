name = "msourceio-inquiry"

# REPLACE these values before publishing
account_id = "REPLACE_WITH_ACCOUNT_ID"
zone_id = "REPLACE_WITH_ZONE_ID"
route = "https://your-domain.com/api/inquiry"
workers_dev = false

# If you prefer to publish to workers.dev, set workers_dev = true and remove route/zone_id

[build]
  # If you need a bundler, set the command here; otherwise leave empty
  command = ""

[vars]
# Example environment variables. For secrets (API keys), use `wrangler secret put <NAME>` instead of placing them here.
INQUIRY_TO_EMAIL = "1476080750@qq.com"
EMAIL_PROVIDER = "mailchannels"
